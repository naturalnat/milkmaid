<% if @orders %>
  <% @orders.each_with_index do |order, index| %>
    <%= index+1 %>. <%= order.name %> <%= link_to "show", order_path(order) %> <br>
  <% end %>
<% else %><br>
  No orders currently placed.
<%end %>

<%= link_to "Place an Order", new_order_path %>